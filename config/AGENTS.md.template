# AGENTS.md

> This file provides universal context for all AI coding agents (Claude Code, Codex CLI, Cursor, Copilot, Gemini CLI, etc.)
> For Claude Code specific instructions, see CLAUDE.md

## Project Overview

<!-- Project-specific context will be added by /init command -->

## Architecture

- See `docs/` for detailed documentation
- See `docs/adr/` for Architecture Decision Records

## Code Conventions

- Follow rules defined in `.claude/rules/` (also applicable to other agents)
- Run linter before committing
- Tests must pass before pushing

## Key Paths

| Path | Purpose |
|------|---------|
| `.claude/rules/` | Coding rules and conventions |
| `.claude/skills/` | Agent skill modules |
| `.claude/agents/` | Specialized agent definitions |
| `docs/` | Project documentation |
| `docs/adr/` | Architecture Decision Records |

## Boundaries

- Do not modify files outside the project root without explicit permission
- Do not add dependencies without approval
- Do not remove or skip existing tests
