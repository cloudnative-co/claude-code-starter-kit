# Git Workflow

- Inspect the full diff before committing or opening a PR.
- Keep commits focused and use the repository's existing commit style.
- Do not rewrite, reset, or discard user changes unless explicitly asked.
- For PRs, summarize user-facing changes, risks, and the exact checks run.
